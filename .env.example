# Database Configuration (External Database)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/inference_instances
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=inference_instances
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# Database Connection Pool Settings
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_RECYCLE=3600
DB_CONNECT_TIMEOUT=10

# Database Debugging
DB_ECHO=false

# Application Settings
ENVIRONMENT=production
DEBUG=false
LOG_TO_FILE=false
SECRET_KEY=your-secret-key-change-in-production
LOG_LEVEL=INFO

# Backend Configuration
BACKEND_PORT=8000
API_BASE_URL=http://localhost:8000

# Frontend Configuration
FRONTEND_PORT=3000
VITE_PORT=5173
VITE_API_BASE_URL=http://localhost:8000

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000

# Authentication Configuration
AUTH_USERNAME=admin
# Use hashed password instead of plain text for security
# Generate hash using: python -c "from password_utils import generate_password_hash; print(generate_password_hash('your-password'))"
AUTH_PASSWORD_HASH=$2b$12$UKaYdrSkZRYQe8DtbjnLa.D37NzLS.4PNInZIzRlAubyyKe7sb/dy
AUTH_RATE_LIMIT=10
AUTH_SESSION_TIMEOUT=3600
AUTH_MAX_RETRY_ATTEMPTS=3